import * as dateUtils from '../../components/date-picker/util';

export const {
    toDate,
    clearHours,
    isInRange,
    formatDate,
    parseDate,
    getDayCountOfMonth,
    getFirstDayOfMonth,
    siblingMonth,
    prevMonth,
    nextMonth,
    initTimeDate,
    formatDateLabels,
    DEFAULT_FORMATS,
    TYPE_VALUE_RESOLVER_MAP
} = dateUtils;
export default dateUtils;
